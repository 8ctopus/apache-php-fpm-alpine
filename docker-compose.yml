version: '3.7'

services:
  web:
    # php 8.2.0 RC6 with virtual hosts
    image: 8ct8pus/apache-php-fpm-alpine:2.0.4
    # php 8.2.0 RC6
    #image: 8ct8pus/apache-php-fpm-alpine:1.4.4
    # php 8.1.10
    #image: 8ct8pus/apache-php-fpm-alpine:1.3.6
    # php 8.0.17
    #image: 8ct8pus/apache-php-fpm-alpine:1.2.8
    # php 7.4.21
    #image: 8ct8pus/apache-php-fpm-alpine:1.1.3
    # development image
    #image: apache-php-fpm-alpine:dev
    hostname: testing
    container_name: web
    ports:
      - 80:80
      - 443:443
    volumes:
      # development directory
      - ./sites/:/sites/
      # expose apache2 and php config to host
      - ./docker/etc/:/docker/etc/
      # expose logs to host
      #- ./docker/log/:/var/log/
